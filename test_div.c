#include <stdio.h>
#include <stdlib.h>

char** test_div (char** div1, char** div2, int length_d1, int length_d2)
{
    int i;
    int j;
    short bol = 0;
    r
    for ( i = 0 ; i < length_d1 , i++)
    {
        for ( j = 0 ; j < length_d2, j++ )
        {
            if (div1[i] != div2[j])
            {
                bol = 1;
            }
        }
    }
    if ( 0 == bol ) {
        char result[length_d1 + legnth_d2][200]reunite(div, div2);
        return result;
    }
    else
    {
        return NULL;
    }
}

char** reunite (char** tab1, char** tab2, char* title1, char* title2,int lengtht1, int lengtht2, int length1, int length2)
{
    char result[length1 + length2][200];
    int i, j, k;
    for ( i = 0 ; i <  length1 + length2 ; i++)
    {
        for ( j = 0 ; j < lengtht1 + )
    }

}
